---
include:
  - al_package.yml
---
mandatory: True
code: |
  # basic_questions_signature_flow
  al_user_bundle
  end_screen
---
need: al_user_bundle
id: test end screen
event: end_screen
question: |
  Hello, ${ users }
subquestion: |
  #### Your files are ready to download below
  ${ al_user_bundle.download_list_html() }
  
  [Click a link](${ url_action('al_user_bundle.send_email_action_event', editable=True, email="qsteenhuis@suffolk.edu") })
  
  ${ al_user_bundle.send_button_html() }
---
objects:
  - dummy_template: ALDocument.using(title="Dummy template", filename="dummy_template", has_addendum=False, enabled=True)
---
objects:
  - al_user_bundle: ALDocumentBundle.using(elements=[dummy_template], filename="user_bundle.pdf", title="All forms to download for your records")
---
attachment:
  variable name: dummy_template[i]
  content: |
    You live at ${ users[0].address.on_one_line() }

    You have { len(children) } children, named { children.familiar() }
    
    Signed,
#    % if i == 'final':
#    ${ users[0].signature }
#    % endif
---
generic object: ALDocumentBundle
template: x.send_email_template
subject: |
  Subject
content: |
  Here is your email
---
#generic object: ALDocumentBundle
#event: x.send_email_action_event
#code: |
#  message("Hello")